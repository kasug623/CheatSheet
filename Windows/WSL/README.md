# TOC
- [start](#start)

# start
## On cmd.exe or powershell
Use `wsl` or `bash`.  
But if you setup `zsh` on wsl and want to use it from start-up,  
you have to use `wsl`.
```
$ wsl
```
or
```
$ bash
```


